---
description: M1 Sprint 上下文 —— 当前 M1 阶段任务范围、砍项建议、关键路径、待用户确认事项。规划或执行 M1 编码任务时引用。
globs:
alwaysApply: false
---

# M1 Sprint 上下文

> M1（第 1 个月）= 4 周 Sprint，目标跑通"造梗→评分→军团→PK"主闭环 + 合规 gate 就位。详细任务见 `docs/M1-Sprint-Plan.md`。

## M1 必须交付（P0，对齐 PRD §11.1）

- 用户系统：手机号验证码登录 + 兴趣标签 + 基础主页 + 等级 + 3 勋章
- AI 造梗：文本造梗（单次 prompt）+ 图片造梗 + 5 官方模板 + **Pro 造梗 Agent（3 步精简版）**
- AI 视频：文生视频 5/10/15s + 4 音色 + 自动字幕（豆包 Seedance mini，配额免费 1次/周·Pro 3次/日）
- 梗卡 feed：发布 + 推荐 + 评分 + 评论 + 转发 + 举报
- 评分体系：单一总分 + 神/烂梗判定 + 评审官
- 梗大军：创建/加入/主页/等级/贡献度/群聊
- PK：投票 PK + 造梗对决
- IM：私聊 + 军团群聊 + @ + 系统通知
- 商业化：Pro ¥18 + 视频按次包 + 微信支付
- 合规：AIGC 备案材料（M3 上线 gate）+ AI 生成内容标识
- 后台：审核队列 + 用户封禁 + 数据看板（最小）

## ⚠️ 工时现实（必须处理）

- M1 P0 工时 = **74 人日**
- 个人开发者月产能 ≈ **22 人日**
- 缺口 **2.4 倍**，不砍无法完成
- **砍后 M1 P0 ≈ 25 人日**（与产能匹配，保证端到端 Demo 闭环）

## 砍项建议（docs/M1-Sprint-Plan.md §7.3，20 条，待用户拍板）

核心砍项（节省最多）：
1. 图片造梗整链路 → 延 M2（-4.0d）
2. 神/烂梗判定 → 延 M2，M1 仅留阈值占位（-1.5d）
3. OAuth 登录 → M2，M1 仅手机号 + 邮箱（-1.5d）
4. 转发 + 举报 → M2（-1.5d）
5. Expo Update / EAS 配置 → M2（-1.0d）
6. Admin shell 仅留登录页（-1.0d）
7. 17 埋点事件缩减到 8 个核心（-0.7d）
8. TTS/Video Adapter 占位、梗卡向量化、评审官骨架、analytics 看板等 P1 全延 M2

**砍后 M1 核心闭环**：手机号登录 → 文本造梗 3 候选 → 梗卡发布 → 机审 → 推荐 feed → 评分评论 → RN 真机可用 + 合规草稿启动。

## 关键路径（不可延迟）

```
INF(基础设施) → USER(用户/登录) → AIO(AI编排 LLM Adapter) → CRE(造梗任务)
→ MEME(梗卡发布) → AUD(机审) → MEME(feed+详情) → RAT(评分+神烂梗) → RN(评分页) → Demo
```

## M1 退出标准（Exit Criteria）

- [ ] 后端启动 + Swagger UI 可访问 + 健康检查通过
- [ ] 手机号验证码登录闭环可用（发码→登录→JWT）
- [ ] 文本造梗 3 候选生成（DeepSeek 真实接入）
- [ ] 梗卡发布 → 机审 → feed 列表可见
- [ ] 评分 + 评论接口可用
- [ ] RN App 5 Tab 可导航 + 评分弹层可调后端
- [ ] AIGC 备案材料草稿完成
- [ ] 基础设施（PG/Redis/R2/CI）全部就位

## 待用户确认（阻塞 M1 推进）

1. **砍项清单是否接受**：`M1-Sprint-Plan.md §7.3` 20 条砍项建议，是否全部接受？或有调整？
2. **脚手架是否先通电验证**：是否要我代执行 `pnpm install` + `pnpm db:init` + `pnpm dev:backend` 验证脚手架能跑（需你本地先开 Docker Desktop）？
3. **M1 S2 起点**：砍项确认后，从 S2（用户系统 + AI 编排 LLM Adapter 真实接入）开始编码？

## M2 预告

图片造梗、神/烂梗判定、OAuth、转发举报、Expo Update、评审官机制、Admin 完整后台、埋点补全、TTS/Video Adapter 真实接入。
