---
description: 上下文维护协议 —— 定义何时更新 activeContext/decisions/progress，确保跨会话上下文不漂移。每次会话自动加载。
globs:
alwaysApply: true
---

# 上下文维护协议 · Context Maintenance Protocol

> 本规则定义"项目推进时如何维护 `.cursor/rules/` 与 `docs/context/` 上下文文件"。**上下文同步是任务完成的一部分，不是可选动作。**

## 核心原则

1. **静态设计文档 vs 活状态文档分离**：
   - `docs/PRD.md` / `TechnicalDesign.md` / `Database-DDL.md` / `API-Spec.md` / `M1-Sprint-Plan.md` 是**静态设计文档**——只在对应设计变更时改，改前先与用户确认。
   - `docs/context/activeContext.md` / `progress.md` 是**活状态文档**——随项目推进持续更新。
   - `docs/context/decisions.md` 是**冻结文档**——仅在决策变更时改，改前必须与用户讨论。
2. **代码与文档一致**：改表结构要同步改 4 处（schema.sql + Database-DDL.md + Drizzle schema + openapi.yaml）；改接口要同步改 2 处（openapi.yaml + API-Spec.md）；改技术选型要同步改 TechnicalDesign.md + 00-project-context.mdc。
3. **小步同步**：每完成一个可验证的任务就更新 `activeContext.md`，不要攒到会话结束一次性补（容易漏）。

## 何时更新哪个文件（触发规则）

### 完成一个 M1 任务时
→ 更新 `docs/context/activeContext.md`：
- "当前状态"区：把该项从"进行中"移到"已完成"
- "下一步"区：调整下一项任务
- "会话历史"表：加一行（时间 + 任务 + 产出）
→ 更新 `docs/context/progress.md`：
- "已完成清单"勾选对应项
- "总体进度"表更新完成度

### 完成一个 Sprint 周次时
→ 更新 `docs/context/progress.md`：
- "总体进度"表对应 Sprint 状态从"待启动"改为"完成"
- "关键数字速查"如有变化则更新

### 技术选型/架构决策变更时
→ **必须先与用户讨论，确认后才改**：
- `docs/context/decisions.md`：新增 ADR 或修改现有 ADR（状态从 ✅ 改为 🔄 再到 ✅，保留旧决策的"放弃项"说明）
- `docs/TechnicalDesign.md`：对应章节同步改
- `.cursor/rules/00-project-context.mdc`：技术栈表或约定同步改
- `docs/context/activeContext.md`：会话历史加一行记录决策变更

### 数据库表结构变更时
→ 同步改 4 处（缺一不可）：
1. `docs/db/schema.sql`（权威 DDL）
2. `docs/Database-DDL.md`（设计说明对应章节）
3. `apps/backend/src/database/schema.ts`（Drizzle schema）
4. `docs/openapi.yaml`（如涉及接口 DTO 字段）
→ 跑 `pnpm --filter @memestar/backend db:generate` 生成迁移
→ 更新 `docs/context/activeContext.md` 会话历史

### 接口变更时
→ 同步改 2 处：
1. `docs/openapi.yaml`（权威契约）
2. `docs/API-Spec.md`（人类可读详设对应接口）
→ 跑 `pnpm gen:api` 重新生成 `packages/shared/src/api-client/generated.ts`
→ 更新 `docs/context/activeContext.md` 会话历史

### 发现 Bug / 技术债时
→ 更新 `docs/context/progress.md` 的"已知问题/技术债"区
→ 严重的同步到 `docs/context/activeContext.md` 的"阻塞/待用户确认"

### 会话结束前（强制）
→ 更新 `docs/context/activeContext.md`：
- "当前状态"三区（已完成/进行中/待启动）准确反映现状
- "下一步"列清楚下次会话该做什么
- "阻塞/待用户确认"列清需要用户拍板的事
- "最后更新"时间戳改为今天
- "会话历史"表加一行本会话摘要

## Definition of Done（任务完成标准）

一个任务算"完成"必须同时满足：
- [ ] 代码实现通过 `pnpm typecheck` + `pnpm lint`（无新增错误）
- [ ] 涉及的文档已同步（schema/API/设计文档一致）
- [ ] `docs/context/activeContext.md` 已更新本任务状态
- [ ] 如涉及决策变更，`docs/context/decisions.md` 已更新且已与用户确认
- [ ] 如涉及进度里程碑，`docs/context/progress.md` 已更新
- [ ] commit message 符合 conventional commits 规范

**未更新上下文文档的任务不算完成**——下一会话会因上下文漂移而迷失。

## 上下文文件修改权限

| 文件 | 谁能改 | 何时改 |
|---|---|---|
| `docs/context/activeContext.md` | Agent 可自主改 | 每次任务完成/会话结束 |
| `docs/context/progress.md` | Agent 可自主改 | 任务完成/Sprint 完成/发现技术债 |
| `docs/context/decisions.md` | **必须用户确认后改** | 决策变更时 |
| `docs/PRD.md` | **必须用户确认后改** | 产品需求变更时 |
| `docs/TechnicalDesign.md` | **必须用户确认后改** | 架构变更时 |
| `docs/db/schema.sql` | Agent 可改（同步其余 3 处） | 表结构变更时 |
| `docs/openapi.yaml` | Agent 可改（同步 API-Spec.md） | 接口变更时 |
| `.cursor/rules/00-project-context.mdc` | **必须用户确认后改** | 技术栈/约定变更时 |
| `.cursor/rules/10-coding-conventions.mdc` | **必须用户确认后改** | 编码约定变更时 |
| `.cursor/rules/20-m1-sprint.mdc` | Agent 可改（M1 推进时） | M1 任务状态变更时 |

## 上下文漂移自检（Agent 每次会话开头默念）

1. 读 `docs/context/activeContext.md` → 知道上次做到哪
2. 读 `docs/context/progress.md` 的"未完成/待做" → 知道接下来做什么
3. 读 `.cursor/rules/20-m1-sprint.mdc`（如做 M1 任务）→ 知道 Sprint 范围
4. 如发现代码状态与 `progress.md` 不一致 → 主动向用户报告"上下文漂移"，询问以哪个为准

## 上下文压缩（防膨胀）

- `activeContext.md` 的"会话历史"表超过 20 行时，把最旧的 10 行合并为一行摘要（如"2026-07 上旬：完成 M1 S1-S2，详见 git log"）
- `progress.md` 的"已完成清单"超过 50 项时，按 Sprint 折叠为折叠区
- 决不改 `decisions.md` 的历史 ADR（只追加新 ADR 或改状态字段）

## 自动化工具（已就位）

| 工具 | 用法 | 作用 |
|---|---|---|
| `pnpm check:context` | 本地手动跑 | 10 项一致性校验（表数/索引/接口数/ADR 数/规则文件/文档存在性/activeContext 时效） |
| `scripts/check-context-sync.sh` | 直接 bash 跑 | 同上，退出码 0/1/2 |
| `.github/workflows/context-sync.yml` | PR 自动触发 | 跑校验 + 检查 activeContext 是否随源码更新 + 检查受保护文档是否被擅改，结果发 PR 评论 |
| `husky/pre-commit` | git commit 时自动跑 | lint-staged 后跑上下文软检查（警告不阻断，严重错误提示） |
| `/update-context` 斜杠命令 | 对话里输入 | Agent 按 30 协议全量同步上下文（读状态→报告漂移→更新自主可改文件→暂停问用户需确认项→同步设计文档→生成报告） |

**严重漂移（exit 2）会阻断 PR 合并**；警告（exit 1）仅提示不阻断。
