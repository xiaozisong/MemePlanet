// Drizzle schema 占位
// 实际生产建议：用 `drizzle-kit introspect` 从 docs/db/schema.sql 反向生成
// 或直接维护等价的 TS schema，以便类型安全的查询。
// MVP 阶段直接用 raw SQL（schema.sql 已建表），此文件先留空接口。

export const schemaPlaceholder = true;
